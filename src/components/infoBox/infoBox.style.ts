import styled from "styled-components";
import { TextStyled } from "../common/common.style";

export const InfoTitleStyled = styled.h4`
  font-weight: 600;
  font-weight: 600;
  font-size: 1.2em;
  margin: 5px 0 0;
`;
export const InfoSectionStyled = styled(TextStyled)`
  margin: 5px auto;
  font-style: italic;
`;

